# Pi writes session history into `.pi/sessions/` inside each project
# directory (configured via sessionDir in pi-config/settings.json).
# Projects should gitignore their own `.pi/` if they don't want sessions
# in version control.
.pi

# macOS cruft
.DS_Store


